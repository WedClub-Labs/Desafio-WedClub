generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_PROD")
}

model User {
  id       String @id @default(cuid())
  userName String
  email    String @unique
  image    String

  @@map("users")
}
